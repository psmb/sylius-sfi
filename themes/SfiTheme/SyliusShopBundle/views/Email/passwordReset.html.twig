{% block subject %}
    Восстановление пароля на сайте Книги СФИ
{% endblock %}

{% block body %}
    {% set url = channel.hostname is not null ? 'http://' ~ channel.hostname ~ path('sylius_shop_password_reset', { 'token': user.passwordResetToken}) : url('sylius_shop_password_reset', { 'token': user.passwordResetToken}) %}
    {% autoescape %}
        Чтобы сбросить пароль для пользователся {{ user.username }} перейдите по ссылке: <a href="{{ url|raw }}">{{ url|raw }}</a>
        <br/><br/>
        Искренне ваши,<br/>
        команда магазина Книги СФИ
    {% endautoescape %}
{% endblock %}
